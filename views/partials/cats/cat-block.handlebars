<li>
	{{name}}

	<button class="change-sleep" data-id="{{id}}" data-newsleep="{{sleep}}">
		{{#if sleep}}Eat Me!{{else}}Re Order a Good Classic{{/if}}
	</button>

	<button class="delete-cat" data-id="{{this.id}}">Cancel Order</button>
</li>
